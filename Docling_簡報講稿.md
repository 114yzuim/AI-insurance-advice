# Docling 簡報講稿（逐字稿 + 預期問答）

> 對應簡報：`Docling_簡介與專案應用.pptx`（7 頁）
> 總時長約 6–8 分鐘；每頁標有建議秒數。可直接照念，或抓重點口語化。
> 語氣：自然、像在跟老師說明，不要像背稿。**粗體**處可加重語氣或停頓。

---

## 第 1 頁 ─ 標題頁（約 30 秒）

老師好，這一段我想補充說明一個我們系統裡很關鍵、但前面比較沒展開的技術元件 ── **Docling**，也就是我們處理保單 PDF 條款的「文件解析」工具。

我會分成五個部分：先講 **它是什麼**、**核心技術**，再講 **為什麼我們需要它**、**怎麼整合進我們的 RAG 系統**，最後是 **實際成效跟限制**。

簡單說，這次的重點就是這張圖：我們把保單 PDF 從「純文字抽取」，升級成「**保留版面結構**的解析」，讓後面的 AI 真的讀得懂條款的結構。

---

## 第 2 頁 ─ Docling 是什麼（約 60 秒）

先講它是什麼。**Docling 是 IBM Research 開源的一個「文件智慧」工具**，論文是 2024 年 Auer 等人發表的。它的工作很單純：把 PDF 這種文件，解析成「**保留原始版面結構**」的 Markdown 或 JSON，再交給後面的程式或 AI 使用。

這裡有三個重點，也是它跟傳統工具最不一樣的地方：

第一，**它不只是「抽文字」**。一般的 PDF 文字抽取工具，是把文字直接倒出來，但順序跟結構常常會亂掉；Docling 會先「**看懂版面**」，再去抽取內容。

第二，**它理解文件結構**。它認得哪裡是標題、哪裡是段落、哪裡是表格，還有它們之間的層級關係，最後輸出帶有 `#`、`##`、`###` 標題的 Markdown。

第三，**它能正確還原表格**。保單裡有很多費率表、給付比例表，傳統工具會把這些數字打散；Docling 會把它還原成真正的表格。

對我們做保險條款的人來說，這三點都很關鍵。

---

## 第 3 頁 ─ 核心技術（約 70 秒）

它之所以做得到，是因為背後有**兩個專用的 AI 模型在協作**。

第一個是 **DocLayNet，版面分析模型**。它負責辨識「頁面上每個區塊是什麼」── 這塊是標題、這塊是內文、這塊是表格、這塊是圖片，以及它們的位置跟層級。簡單講，它決定的是「**哪裡是標題、哪裡是段落**」。

第二個是 **TableFormer，表格結構辨識模型**。它專門處理表格，把保單 PDF 裡的費率表、給付比例表還原成「列」跟「欄」對齊正確的表格。它決定的是「**表格的欄位怎麼對齊**」。

這兩個模型協作之後的產出，就是下面這條：**一份保留標題層級跟表格格式的結構化 Markdown**。

這個產出很重要，因為它讓我們後面在「切段」的時候，可以用「**語意完整的條款單元**」當單位 ── 比如一整條「除外責任」當一段 ── 而不是像以前那樣，機械式地每 500 個字硬切一刀。

---

## 第 4 頁 ─ 為什麼需要它（約 75 秒）

那為什麼我們要特地換成它？這頁是新舊方案的對比。

左邊是我們**專案初期用的 pdfplumber**。它的問題是：純文字抽取之後，是切成「固定長度 500 字元」的片段。這會造成幾個問題 ── 它**沒辦法辨識表格**，費率表會變成一堆對不起來的數字；它**不認得標題層級**，整個條款的結構就消失了；而且它可能把「除外責任」、「職業限制」這種完整的條款，**從中間硬切斷**。

右邊是我們**現在用的 Docling**。它是依「標題的語意邊界」切段，所以條款單元是完整的;TableFormer 會完整保留費率表的結構;標題層級也都保留;像「除外責任」、「職業限制」這種段落不會被切碎。

最下面我特別想強調 ── **為什麼這件事對「保險條款」特別重要**：因為除外責任、職業類別限制、費率表，剛好就是**影響消費者決策最關鍵的資訊**。一旦這些被切碎，我們的向量檢索就抓不到完整的內容，最後就會直接影響到推薦的「**條款一致性**」，也就是我們論文裡的 D2 指標。

---

## 第 5 頁 ─ 如何整合進專案（約 75 秒）

接下來講我們實際怎麼把它放進系統。這是我們的 RAG pipeline，總共五步：

第一步，我們挑了 **12 份保單 PDF**，是六大險種各兩筆的代表性商品。

第二步，**用 Docling 解析**，其實程式碼就是下面這兩行 ── 呼叫 `DocumentConverter` 的 `convert`，再 `export_to_markdown`，就拿到結構化的 Markdown 了。

第三步，**依標題切段**。我們以 Markdown 的 `#` 到 `####` 標題當邊界切段;如果某一段太長，再以句子邊界切細，而且保留 80 個字的重疊，避免句子被切斷後語意斷掉。

第四步，把這些段落用**多語意嵌入模型**轉成向量，建立索引，最後總共是 **834 個 chunk**。

第五步，使用者查詢的時候，我們檢索出**最相關的 6 段條款**，連同來源商品資訊一起注入 Claude，讓它根據「真實的條款原文」生成推薦,並引用具體條款。

我想強調一點：這次升級**沒有改動任何前端、也沒有改變既有的檢索介面**，我們只是把「文件解析」這一層，從 pdfplumber 換成 Docling。是一個乾淨的模組替換。

---

## 第 6 頁 ─ 實際成效與選型理由（約 70 秒）

那換完之後的成效是什麼？上排三個數字：

第一，**chunk 數量從 464 變成 834**。不是變多就好，而是切段更貼合條款的語意單元。

第二，**平均長度是 355 字元**。重點是它依段落的自然長度浮動，而不再是固定 500，所以更尊重原本的條款結構。

第三，也是最重要的 ── **D2 條款一致性從 2.00 提升到 2.52**。這代表 AI 真的能引用到具體的承保條件、給付比例,而不是只講業界通則。

下排是「**為什麼選 Docling，而不是商用方案**」。主要三個理由：它是**開源、可以本地部署**，資料不用外流;它**沒有逐頁計費**,不像 Azure 的 Document Intelligence 或 LlamaParse 是按頁收費;最後,它**很適合學術原型階段**,符合我們研究的工程資源跟預算限制。

---

## 第 7 頁 ─ 限制與小結（約 50 秒）

最後誠實講一下目前的限制。

**複雜的表格還是可能被解析成「過長的單一 chunk」**,我們這次索引裡最長的一段達到 3,116 個字元,代表表格內部還沒有被進一步切細。所以「**表格列級的切割**」是我們後續要優化的方向。

總結四點:Docling 把我們的文件解析,從「純文字抽取」升級成「**理解版面結構**」;它保留標題層級跟表格,讓 RAG 能用語意完整的條款單元來檢索;它是我們「**條款一致性 D2 提升**」的關鍵基礎建設;而且開源、本地、零逐頁成本,很契合學術原型開發。

**一句話總結就是:Docling 讓 AI「讀懂」保單條款的結構,而不只是讀到文字。**

以上是 Docling 的補充說明,謝謝老師,看老師有沒有什麼問題。

---

# 預期問答（Q&A）

> 以下是教授可能追問的問題與建議回答方向。括號內是可省略的延伸補充。

**Q1. 為什麼不直接用 GPT-4o 或 Claude 的多模態能力去「看」PDF 就好,還要 Docling？**
A:主要是成本、可重現性跟資料隱私。多模態模型逐頁丟進去,API 成本高、而且每次輸出可能不一致;Docling 是專用的解析模型,輸出穩定、可在本地跑、資料不外流,作為**前處理層**更可靠。(而且我們真正要的是「結構化的條款文字」進向量庫,不是每次查詢都重新讀整份 PDF。)

**Q2. Docling 解析的準確度怎麼驗證?會不會解析錯?**
A:目前我們主要是**人工抽查**解析後的 Markdown,確認標題層級跟費率表有正確還原。我也誠實說,我們還沒做大規模的解析正確率量化,這確實是可以補強的地方。不過從下游效果看,D2 條款一致性有實際提升,間接支持解析品質是堪用的。

**Q3. 834 個 chunk、平均 355 字、最長 3116 字,這些參數是怎麼定的?**
A:我們以 Markdown 標題當主要切段邊界,所以長度是「依條款段落自然浮動」,不是我設死的。只有當一段超過設定上限時,才用句子邊界再切細並保留 80 字重疊。最長 3116 字那段是**複雜表格沒被進一步切開**造成的,屬於已知限制。

**Q4. 最長 3116 字的 chunk 會不會影響檢索?**
A:會有影響 ── 太長的 chunk 在向量檢索時語意會被稀釋,精準度下降。這正是我把「表格列級切割」列為後續優化的原因。不過它目前不算多,還沒嚴重到讓 D2 退步。

**Q5. 你們主論文不是說「全文」策略反而比「切割檢索」好嗎?那還需要 Docling 切段?**
A:這是個好問題。在我們**只有 12 份商品**的小規模下,直接把整份條款全文丟給模型確實表現最好,因為全文長度還在模型的上下文窗口內。但這不能外推到原始 **780 筆商品**的規模 ── 那時全文塞不進上下文,一定得靠切段檢索。而且不論走全文還是切段,**Docling 都能讓解析品質更好**,因為它連全文的結構都保留得更乾淨。所以 Docling 的價值跟「全文 vs 切割」的選擇是獨立的。

**Q6. Docling 對中文 PDF 的支援好嗎?它是不是以英文為主?**
A:Docling 的版面分析跟表格辨識主要是看**版面幾何結構**,跟語言比較無關,所以中文保單的標題、表格一樣能辨識。後面的語意嵌入,我們用的是**多語言模型**(paraphrase-multilingual-MiniLM),本來就支援中文。(實務上中文條款解析結果是可用的,費率表也有正確還原。)

**Q7. 解析 12 份要跑多久?會是效能瓶頸嗎?**
A:Docling 首次解析會載入模型,12 份大約一兩分鐘,但這是**離線一次性的索引建立**,不是每次查詢都跑。線上查詢只做向量檢索,延遲很低,所以不是瓶頸。

**Q8. 那如果要擴大到 780 筆全部商品,這套還行得通嗎?**
A:解析端沒問題,Docling 可以批次跑。真正要重新評估的是**檢索策略** ── 780 筆規模下全文塞不下,得回到切段檢索,可能還要加一層「先用 metadata 篩候選、再對少數候選提供全文」的混合策略。這也是我論文未來工作裡列的方向之一。

**Q9. 為什麼是選這 12 份?代表性夠嗎?**
A:我們是依六大險種類別各抽兩筆代表性商品,目的是讓 PDF 條款層的實驗能涵蓋主要險種。我也誠實說,12 份相對 780 份是小樣本,代表性是這份研究的限制之一,後續會擴大。

**Q10.(若被問成本)Docling 完全免費嗎?**
A:Docling 本身開源免費,沒有授權或逐頁費用,只需要自己的運算資源。相對 Azure Document Intelligence、LlamaParse 那種按頁計費的商用服務,對學術原型省很多。

---

## 結尾備忘
- 若時間緊,可只講第 2、4、5、6 頁(是什麼/為什麼/怎麼用/成效),第 3 頁技術細節跟第 7 頁限制可口頭帶過。
- 被問到不確定的數字,就誠實說「這部分我回去確認」,不要硬掰。
